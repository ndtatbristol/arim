# Dry-run configuration file
# Use this configuration file to check the computation runs
# as expected.
#
# To use this configuration file, call the script with flag --dryrun.
# If used, this configuration file is applied at the top of conf.yaml.

interfaces.frontwall:
  numpoints: 500
interfaces.backwall:
  numpoints: 500
interfaces.grid:
  pixel_size: 2.e-3

tfm.use_dynamic_scale: true

views_to_use: [L-L, LL-T, LT-L, TL-TT]

plot.force_close: false
plot.show_rays: true
